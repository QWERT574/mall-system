<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.minimall.mapper.AfterSaleServiceMapper">
    <select id="selectByUserId" resultType="com.example.minimall.model.AfterSaleService">
        SELECT * FROM after_sale_service WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="selectByOrderId" resultType="com.example.minimall.model.AfterSaleService">
        SELECT * FROM after_sale_service WHERE order_id = #{orderId} ORDER BY created_at DESC
    </select>

    <select id="selectByProductId" resultType="com.example.minimall.model.AfterSaleService">
        SELECT * FROM after_sale_service WHERE product_id = #{productId} ORDER BY created_at DESC
    </select>
</mapper>