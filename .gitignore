# ================================
# 项目级忽略规则
# ================================

# 备份目录
backup_*/
backup_*/
*.bak
*.backup
*.bkp

# 运行时数据 (用户上传/产物/缓存, 应放在源码外)
data/
**/uploads/
!**/uploads/.gitkeep
!**/static/images/

# 日志文件
**/*.log
**/logs/
**/logs/*
!logs/.gitkeep

# 构建产物
**/target/
**/dist/
**/build/

# 前端构建产物拷贝（由 CI 从源码构建，不入库）
# 注意：根目录 assets/ 与根 index.html 是 Pages 落地页的发布产物，
# 被 .github/workflows/deploy-pages.yml 的 Assemble site 步骤直接消费（cp 到 site/），
# 必须保持 git 跟踪，不可 untrack；仓库画像分析应把它们当产物而非源码
web-mall/assets/
admin-web/assets/
seller-web/assets/
web-mall-dist/
admin-web-dist/
seller-web-dist/

# 依赖目录
**/node_modules/

# 本地 AI 助手工作目录（会话状态/报告，不入库）
.qoder/
.hermes/
.workbuddy/
.venv/

# 误生成的运行时输出
bash.exe.stackdump
/activity
**/compile_errors.txt

# 环境配置文件（保留示例文件）
**/.env
!**/.env.example

# 编译产物
**/*.class
**/*.pyc
**/__pycache__/

# 系统文件
.DS_Store
Thumbs.db
desktop.ini

# IDE 配置
.vscode/
.idea/
.trae/
*.iml
*.ipr
*.iws

# 临时文件
*.tmp
*.temp
*.swp
*.swo
*~

# 误创建的运行时产物（curl/PowerShell 把响应写到文件名里）
gateway/[0-9]*
gateway/[0-9]*/*

# 测试文件（可选）
# **/test_*.sql
# **/*_test.sql

# ================================
# 后端特定规则 (Spring Boot + Maven)
# ================================

# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# Spring Boot
application-*.properties
application-*.yml
!application.yml
!application.properties

# 数据库备份
*.sql.backup
*_backup_*.sql

# ================================
# 前端特定规则 (Vue + Vite/Webpack)
# ================================

# 构建输出
dist/
build/
*.local

# 依赖
node_modules/
npm-debug.log
yarn-error.log
pnpm-debug.log

# 开发环境配置
.env.local
.env.*.local

# 编辑器
*.sublime-project
*.sublime-workspace

# ================================
# 微信小程序特定规则
# ================================

# 开发者工具配置
project.private.config.json
project.private.config.js

# 编译产物
**/.tea/
**/miniprogram_dist/

# ================================
# 数据库相关
# ================================

# 数据库导出文件（大文件）
*.sql.gz
*.sql.bz2

# 临时 SQL 文件（可选）
# *_temp.sql
# *_fix.sql
# *_migration_temp.sql

# ================================
# 文档和报告（可选）
# ================================

# 临时文档
*_draft.md
*_temp.md
*_backup.md

# 报告（保留重要报告，忽略临时报告）
# *_temp_report.md
# *_draft_report.md

# ================================
# 保留重要示例文件
# ================================

# 环境变量示例
!.env.example

# 配置示例
!**/example/
!**/examples/

# 模板文件
*.template
*.tpl
