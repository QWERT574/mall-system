<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.minimall.mapper.ShippingAddressMapper">
    <select id="selectDefault" resultType="com.example.minimall.model.ShippingAddress">
        SELECT * FROM shipping_address WHERE is_default = 1
    </select>
</mapper>