<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.minimall.mapper.ConversationSessionMapper">

    <select id="selectByToken" resultType="com.example.minimall.model.ConversationSession">
        SELECT * FROM conversation_session WHERE session_token = #{sessionToken}
    </select>

    <select id="selectActiveByUserId" resultType="com.example.minimall.model.ConversationSession">
        SELECT * FROM conversation_session WHERE user_id = #{userId} AND status = 1 ORDER BY updated_at DESC
    </select>

    <update id="updateContextSummary">
        UPDATE conversation_session
        SET context_summary = #{contextSummary}, message_count = #{messageCount}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="closeSession">
        UPDATE conversation_session SET status = 0, updated_at = NOW() WHERE id = #{id}
    </update>
</mapper>
