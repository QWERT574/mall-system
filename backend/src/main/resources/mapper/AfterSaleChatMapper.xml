<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.minimall.mapper.AfterSaleChatMapper">
    
    <!-- 根据售后服务ID查询聊天记录 -->
    <select id="selectByAfterSaleId" resultType="com.example.minimall.model.AfterSaleChat">
        SELECT id, after_sale_id as afterSaleId, sender_id as senderId, sender_type as senderType,
               content, message_type as messageType, is_read as isRead, created_at as createdAt, updated_at as updatedAt
        FROM after_sale_chat
        WHERE after_sale_id = #{afterSaleId}
        ORDER BY created_at ASC
    </select>
    
    <!-- 更新聊天记录为已读状态 -->
    <update id="updateChatReadStatus">
        UPDATE after_sale_chat
        SET is_read = 1
        WHERE after_sale_id = #{afterSaleId}
        AND sender_type != #{receiverType}
        AND is_read = 0
    </update>
    
    <!-- 查询未读消息数量 -->
    <select id="selectUnreadCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM after_sale_chat
        WHERE after_sale_id = #{afterSaleId}
        AND sender_type != #{receiverType}
        AND is_read = 0
    </select>
</mapper>