<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.minimall.mapper.ConversationMessageMapper">

    <select id="selectRecentBySessionId" resultType="com.example.minimall.model.ConversationMessage">
        SELECT * FROM (
            SELECT * FROM conversation_message WHERE session_id = #{sessionId} ORDER BY created_at DESC LIMIT #{limit}
        ) t ORDER BY created_at ASC
    </select>

    <select id="selectBySessionId" resultType="com.example.minimall.model.ConversationMessage">
        SELECT * FROM conversation_message WHERE session_id = #{sessionId} ORDER BY created_at ASC
    </select>
</mapper>
